# Screenshots

Drop PNGs here with the filenames below to have them show up in the main README.

| Filename | Suggested aspect | What to capture |
|---|---|---|
| `01-viewer.png` | 16:10 | Main 3-column layout with `plasma` or `audio-bars` running; bloom on; front camera preset. |
| `02-pattern.png` | 16:10 | Same, but with a pattern file visible in an external editor + the Library panel showing it selected. Annotate the hot-reload flow if you like. |
| `03-controllers.png` | 4:3 | Controllers panel with at least one auto-assigned topology and at least one deliberate coverage issue visible. |
| `04-export.png` | 4:3 | Export panel with the status line showing a recent successful export. |

Tips:
- Use a ~1600px-wide window so the PNGs render sharply on retina displays.
- Black background + bloom-enabled dome photographs better than the idle state.
- A short GIF loop of `tilt-level` under the `rocking` motion source is a good hero asset — save as `hero.gif` and reference it above the first table.
